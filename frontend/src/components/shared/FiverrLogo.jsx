function FiverrLogo({ fillColor }) {
  return (
    <span className="inline-flex items-baseline">
      <span
        className="text-3xl font-extrabold tracking-tight select-none"
        style={{ color: fillColor }}
      >
        workerr
      </span>
      <span className="ml-1 mb-0 w-2 h-2 bg-[#1DBF73] rounded-full" />
    </span>
  );
}

export default FiverrLogo;
